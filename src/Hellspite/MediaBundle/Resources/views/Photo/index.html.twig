<h1>Photo list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Image</th>
            <th>Caption</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_photo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.image }}</td>
            <td>{{ entity.caption }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_photo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_photo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_photo_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
